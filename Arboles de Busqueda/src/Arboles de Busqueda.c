
//PE1: Write a program to insert and delete values from
// 	   a binary tree
//PE2: Write a program to count the number of nodes in
//	   a binary search tree

#include <stdio.h>
#include <stdlib.h>

enum bool{true,false};

typedef struct nodo{
	int valor;
	int FE;
	struct nodo *izq;
	struct nodo *der;
} nodo;

int main(void) {

	return 0;
}
